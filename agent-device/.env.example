# 服务端连接配置
# 设置远程升级服务器的URL地址
SERVER_URL=http://localhost:3000

# 设备配置
# 设置设备名称，用于在管理后台中标识该设备
DEVICE_NAME=测试设备

# 设置是否使用真实主机名（不添加进程ID后缀）
# true: 使用真实主机名，如 "ClaudedeMacBook-Air"
# false: 添加进程ID后缀，如 "ClaudedeMacBook-Air-50930"
USE_REAL_HOSTNAME=false

# 多实例环境下的实例ID（可选）
# 如果设置了此值，设备名称将使用此ID而非进程ID作为后缀
# AGENT_INSTANCE_ID=instance-001

# 优先使用配置的设备名称而不是系统主机名
# true: 优先使用 DEVICE_NAME 配置，如 "Claude的MacBook Air"
# false: 优先使用系统主机名，如 "ClaudedeMacBook-Air"
PREFER_CONFIG_NAME=false

# 日志配置
# 日志级别: debug, info, warn, error
LOG_LEVEL=info

# 路径安全配置
# 允许的部署路径，多个路径用逗号分隔
# 示例：ALLOWED_DEPLOY_PATHS=/opt/myapp,/data/deployments,/home/user/apps
# ALLOWED_DEPLOY_PATHS=/Users/claude/CompanyProjects/temp_project

# 生产环境配置示例
# SERVER_URL=https://your-production-server.com
# DEVICE_NAME=生产设备-001
# LOG_LEVEL=warn
# ALLOWED_DEPLOY_PATHS=/opt/production-app,/data/apps