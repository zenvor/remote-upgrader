# 远程升级系统环境配置示例
# 复制为 .env 文件并修改相应值

# 服务器配置
# 监听地址，0.0.0.0 允许外网访问，127.0.0.1 仅本机访问
HOST=0.0.0.0
# 服务器端口
PORT=3000

# 允许的跨域来源，多个用逗号分隔
ALLOWED_ORIGINS=http://localhost:3001,http://127.0.0.1:3001,http://localhost:3000

# 设备文件存储路径
DEVICES_CONFIG_PATH=./config/devices.json

# 上传文件存储路径
UPLOAD_PATH=./uploads

# 日志级别 (error, warn, info, debug)
LOG_LEVEL=info

# 心跳超时时间（毫秒）
HEARTBEAT_TIMEOUT=60000

# 最大连接历史记录数
MAX_CONNECTION_HISTORY=20

# 最大升级历史记录数
MAX_UPGRADE_HISTORY=10

# 是否自动清理离线设备
AUTO_CLEANUP_OFFLINE_DEVICES=false

# 离线设备清理天数
OFFLINE_CLEANUP_DAYS=7

# 最大设备数量限制（防止内存泄漏）
MAX_DEVICES=1000

# 清理间隔（毫秒，默认30分钟）
CLEANUP_INTERVAL=1800000

# 文件上传大小限制（字节，默认500MB）
MAX_FILE_SIZE=524288000

# 数据同步间隔（毫秒，默认5秒）
SYNC_INTERVAL=5000

# 是否使用新版设备管理器
USE_NEW_DEVICE_MANAGER=false

# 默认重试延迟（毫秒）
DEFAULT_RETRY_DELAY=1000

# Swagger API 文档服务器地址
SWAGGER_SERVER_URL=http://localhost:3000

# 测试服务器URL
TEST_SERVER_URL=http://localhost:3000